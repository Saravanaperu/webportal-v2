# Environment variables for the backend

# This is the connection string for your PostgreSQL database.
# Replace the user, password, host, port, and database name with your own.
DATABASE_URL="postgresql://user:password@host:port/database"

# This is the secret key for signing JWTs.
# Replace with a long, random string in production.
JWT_SECRET="a-very-secret-key"
